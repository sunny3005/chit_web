# Supabase project settings -> API
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Supabase project settings -> Database -> Connection string
# DATABASE_URL: pooled connection used at runtime (Prisma Client)
# DIRECT_URL: direct connection used for migrations (npx prisma migrate)
DATABASE_URL=
DIRECT_URL=
